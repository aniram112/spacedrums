//
//  ViewController.swift
//  spacedrums
//
//  Created by Marina Roshchupkina on 25.01.2024.
//

import UIKit
import SwiftUI

class ViewController: UIViewController {

    override func viewDidLoad() {
        super.viewDidLoad()
        view.backgroundColor = .cyan
        /*let vc = UIHostingController(TunerView())
        guard let swiftuiView = vc.view else { return }
        swiftuiView.translatesAutoresizingMaskIntoConstraints = false
        addChild(vc)
        view.addSubview(swiftuiView)
        NSLayoutConstraint.activate([
            swiftuiView.centerXAnchor.constraint(equalTo: view.centerXAnchor),
            swiftuiView.centerYAnchor.constraint(equalTo: view.topAnchor, constant: 200),
        ])
        swiftuiView.backgroundColor = .clear*/
        // Do any additional setup after loading the view.
    }


}

